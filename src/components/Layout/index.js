import { Outlet } from 'react-router-dom';
import Navbar from '../Navbar';
import './Layout.scss';

const Layout = () => {
    return (
     <>
       <Navbar/>
     </>
    )
}

export default Layout;